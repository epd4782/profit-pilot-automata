
{
  "build": {
    "env": {},
    "commands": [
      "npm install",
      "npm run build",
      "cd server && npm install --only=production"
    ]
  },
  "start": {
    "cmd": "node server/index.js"
  }
}
