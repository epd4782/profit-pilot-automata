
{
  "build": {
    "env": {},
    "commands": [
      "npm install",
      "npm run build",
      "cd server && npm install --omit=dev"
    ]
  },
  "start": {
    "cmd": "node server/index.js"
  },
  "healthcheck": {
    "path": "/api/health",
    "timeout": 300
  }
}
